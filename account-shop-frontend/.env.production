# 🌐 Frontend Production Environment
# Copy this to server as .env.local

NEXT_PUBLIC_API_URL=https://api.dinhquocviet.space/api
NEXT_PUBLIC_APP_URL=https://dinhquocviet.space

# Optional: Analytics
NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX

# Build optimization
NODE_ENV=production
# Laravel scheduler
* * * * * cd /home/admin/domains/api.dinhlamthanh.space && php artisan schedule:run >> /dev/null 2>&1

# Clean expired auto deposits (chạy mỗi 5 phút)
*/5 * * * * cd /home/admin/domains/api.dinhlamthanh.space && php artisan queue:work --timeout=60 >> /dev/null 2>&1

# Backup database (chạy hàng ngày lúc 2:00 AM)
0 2 * * * mysqldump -u admin_dinhlamthanhshop -p'TwfnkKLy6NX9NEPNfdwAaGASDlkaerhajkJKAkh' admin_dinhquocv > /home/admin/backups/db_$(date +\%Y\%m\%d).sql

# Clean old backups (giữ 7 ngày)
0 3 * * * find /home/admin/backups -name "db_*.sql" -mtime +7 -delete